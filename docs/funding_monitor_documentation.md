# Документация: Funding Rate Monitor

## Оглавление
1. [Обзор](#обзор)
2. [Назначение](#назначение)
3. [Принцип работы](#принцип-работы)
4. [Архитектура](#архитектура)
5. [Конфигурация](#конфигурация)
6. [Функции и методы](#функции-и-методы)
7. [Расчеты и метрики](#расчеты-и-метрики)
8. [Примеры использования](#примеры-использования)

---

## Обзор

**Funding Rate Monitor** - это скрипт для мониторинга и анализа ставок финансирования (funding rates) на криптовалютных биржах MEXC и Binance. Скрипт автоматически отслеживает разницу в ставках между биржами и отправляет уведомления в Telegram о потенциально прибыльных арбитражных возможностях.

### Ключевые возможности:
- 📊 Мониторинг funding rates на MEXC и Binance
- 📈 Расчет спреда между биржами
- 🔔 Автоматические уведомления в Telegram
- ✅ Определение прибыльных торговых возможностей
- 📱 Форматированные сообщения с эмодзи и HTML-разметкой

---

## Назначение

### Что такое Funding Rate?

**Funding Rate (ставка финансирования)** - это периодический платеж между трейдерами, торгующими бессрочными фьючерсными контрактами (perpetual futures). 

- **Положительная ставка**: Трейдеры в длинных позициях (long) платят трейдерам в коротких позициях (short)
- **Отрицательная ставка**: Трейдеры в коротких позициях платят трейдерам в длинных позициях

### Зачем нужен мониторинг?

Когда funding rates на разных биржах значительно отличаются, возникает **арбитражная возможность**:
- Открыть позицию на одной бирже
- Открыть противоположную позицию на другой бирже
- Получать разницу в funding rates без рыночного риска

---

## Принцип работы

### Алгоритм работы:

```
1. Подключение к биржам MEXC и Binance
   ↓
2. Получение текущих funding rates для символа (APR/USDT:USDT)
   ↓
3. Расчет спреда (разницы) между ставками
   ↓
4. Анализ прибыльности:
   - Если |spread| >= THRESHOLD → Прибыльно ✅
   - Если |spread| < THRESHOLD → Не прибыльно ❌
   ↓
5. Определение торговой стратегии:
   - Если MEXC > Binance → SHORT MEXC / LONG Binance
   - Если Binance > MEXC → LONG MEXC / SHORT Binance
   ↓
6. Формирование и отправка сообщения в Telegram
```

---

## Архитектура

### Структура кода:

```python
funding_monitor.py
├── Конфигурация (строки 7-20)
│   ├── API ключи бирж
│   ├── Telegram настройки
│   └── Параметры мониторинга
│
├── Функции инициализации (строки 23-39)
│   └── init_exchanges() - создание объектов бирж
│
├── Утилиты форматирования (строки 42-94)
│   ├── format_percentage() - форматирование в проценты
│   └── create_message() - создание сообщения для Telegram
│
├── Telegram интеграция (строки 97-108)
│   └── send_telegram_message() - отправка уведомлений
│
└── Основная логика (строки 111-146)
    └── main() - главная асинхронная функция
```

### Зависимости:

```
ccxt          → Работа с биржами
asyncio       → Асинхронное выполнение
telegram      → Отправка уведомлений
datetime      → Работа с временем
```

---

## Конфигурация

### Основные параметры:

#### 1. API ключи бирж

```python
MEXC_API_KEY = 'ваш_ключ'
MEXC_API_SECRET = 'ваш_секрет'

BINANCE_API_KEY = 'ваш_ключ'
BINANCE_API_SECRET = 'ваш_секрет'
```

**⚠️ ВАЖНО**: Эти ключи должны храниться в переменных окружения, а не в коде!

#### 2. Telegram настройки

```python
TELEGRAM_BOT_TOKEN = 'токен_от_BotFather'
TELEGRAM_CHAT_ID = 'ваш_chat_id'
```

**Как получить**:
- `BOT_TOKEN`: Создайте бота через [@BotFather](https://t.me/BotFather)
- `CHAT_ID`: Используйте [@userinfobot](https://t.me/userinfobot)

#### 3. Торговые параметры

```python
SPREAD_THRESHOLD = 0.0005  # 0.05% - минимальный прибыльный спред
SYMBOL = 'APR/USDT:USDT'   # Торговая пара
```

**Настройка порога**:
- `0.0005` = 0.05% - консервативный подход
- `0.001` = 0.1% - умеренный подход
- `0.002` = 0.2% - агрессивный подход

---

## Функции и методы

### 1. `init_exchanges()`

**Назначение**: Инициализация подключений к биржам MEXC и Binance.

**Возвращает**: Кортеж `(mexc, binance)` с объектами бирж.

**Параметры бирж**:
- `timeout`: 30000 мс (30 секунд)
- `enableRateLimit`: True (автоматическое ограничение запросов)

---

### 2. `format_percentage(value)`

**Назначение**: Преобразование десятичного числа в процентный формат.

**Параметры**:
- `value` (float): Десятичное значение (например, 0.0005)

**Возвращает**: Строку в формате "0.0500%"

**Примеры**:
```python
format_percentage(0.0005)  # → "0.0500%"
format_percentage(0.001)   # → "0.1000%"
format_percentage(-0.0003) # → "-0.0300%"
```

---

### 3. `create_message(mexc_rate, binance_rate, spread)`

**Назначение**: Создание форматированного сообщения для Telegram с анализом funding rates.

**Параметры**:
- `mexc_rate` (float): Ставка финансирования на MEXC
- `binance_rate` (float): Ставка финансирования на Binance
- `spread` (float): Разница между ставками

**Возвращает**: HTML-форматированную строку для Telegram

**Логика работы**:

1. **Определение прибыльности**:
```python
abs_spread = abs(spread)
is_profitable = abs_spread >= SPREAD_THRESHOLD
```

2. **Выбор эмодзи и статуса**:
```python
if is_profitable:
    emoji = "🟢"
    status = "PROFITABLE"
else:
    emoji = "🔴"
    status = "NOT PROFITABLE"
```

3. **Определение торговой стратегии**:
```python
if spread > 0:  # mexc > binance
    direction = "SHORT MEXC / LONG Binance"
else:  # binance > mexc
    direction = "LONG MEXC / SHORT Binance"
```

---

### 4. `send_telegram_message(message)`

**Назначение**: Асинхронная отправка сообщения в Telegram.

**Параметры**:
- `message` (str): HTML-форматированное сообщение

**Возвращает**: None

---

### 5. `main()`

**Назначение**: Главная асинхронная функция, координирующая весь процесс мониторинга.

**Алгоритм**:

1. Инициализация бирж
2. Получение funding rates
3. Расчет спреда
4. Вывод в консоль
5. Отправка уведомления в Telegram

---

## Расчеты и метрики

### 1. Funding Rate (Ставка финансирования)

**Что показывает**: Процент, который платят/получают трейдеры каждые 8 часов.

**Формат**: Десятичное число (например, 0.0005 = 0.05%)

**Интерпретация**:
- `> 0`: Лонги платят шортам (рынок в лонге)
- `< 0`: Шорты платят лонгам (рынок в шорте)
- `≈ 0`: Баланс между лонгами и шортами

**Пример**:
```
Funding Rate = 0.0005 (0.05%)
Позиция: $10,000
Платеж каждые 8 часов: $10,000 × 0.0005 = $5
```

---

### 2. Spread (Спред)

**Формула**:
```
Spread = Funding_Rate_MEXC - Funding_Rate_Binance
```

**Что показывает**: Разницу в ставках между биржами.

**Примеры расчета**:

#### Пример 1: Положительный спред
```
MEXC:    0.08% (0.0008)
Binance: 0.03% (0.0003)
Spread:  0.05% (0.0005)

Стратегия: SHORT MEXC / LONG Binance
Прибыль:   0.05% каждые 8 часов
```

#### Пример 2: Отрицательный спред
```
MEXC:    0.02% (0.0002)
Binance: 0.07% (0.0007)
Spread: -0.05% (-0.0005)

Стратегия: LONG MEXC / SHORT Binance
Прибыль:   0.05% каждые 8 часов
```

---

### 3. Расчет потенциальной прибыли

**Формула**:
```
Прибыль_за_период = Позиция × |Spread| - Комиссии
Прибыль_в_день = Прибыль_за_период × (24 / 8)
Прибыль_в_месяц = Прибыль_в_день × 30
```

**Пример расчета**:
```
Позиция:  $10,000
Spread:   0.06% (0.0006)
Комиссии: 0.04% (0.0004)

Прибыль за 8 часов:
$10,000 × (0.0006 - 0.0004) = $2

Прибыль в день:
$2 × 3 = $6

Прибыль в месяц:
$6 × 30 = $180

ROI в месяц:
($180 / $10,000) × 100 = 1.8%
```

---

## Примеры использования

### Запуск скрипта

#### Одноразовый запуск:
```bash
python funding_monitor.py
```

#### Периодический запуск (каждые 5 минут):
```bash
# Linux/Mac (crontab)
*/5 * * * * cd /path/to/project && python funding_monitor.py
```

---

### Примеры вывода

#### Пример 1: Прибыльная возможность
```
Получаем funding rates...

fundingRate mexc: 0.0008
fundingRate binance: 0.0002
fundingRate spread: 0.0006
Spread %: 0.0600%
✓ Сообщение отправлено в Telegram
```

---

## Рекомендации по безопасности

### 1. Хранение API ключей

**❌ Плохо**:
```python
MEXC_API_KEY = 'mx0vglpKet4seDX5h4'  # В коде
```

**✅ Хорошо**:
```python
import os
from dotenv import load_dotenv

load_dotenv()

MEXC_API_KEY = os.getenv('MEXC_API_KEY')
MEXC_API_SECRET = os.getenv('MEXC_API_SECRET')
```

**.env файл**:
```
MEXC_API_KEY=your_key_here
MEXC_API_SECRET=your_secret_here
BINANCE_API_KEY=your_key_here
BINANCE_API_SECRET=your_secret_here
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

**.gitignore**:
```
.env
*.log
__pycache__/
```

---

### 2. Ограничение прав API ключей

На биржах настройте API ключи с минимальными правами:
- ✅ Чтение данных (Read)
- ❌ Торговля (Trade) - не нужно для мониторинга
- ❌ Вывод средств (Withdraw) - никогда не включайте

---

## Заключение

**Funding Rate Monitor** - это мощный инструмент для поиска арбитражных возможностей на криптовалютных биржах. Скрипт автоматизирует процесс мониторинга и уведомления, позволяя трейдерам быстро реагировать на прибыльные ситуации.

### Ключевые преимущества:
- ✅ Автоматический мониторинг 24/7
- ✅ Мгновенные уведомления в Telegram
- ✅ Простота настройки и использования
- ✅ Расширяемость и гибкость
